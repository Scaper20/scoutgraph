# Copy to .env.local and fill in from your CognoDB Cloud instance
# (console.cognodb.com). Never commit the real file.

COGNODB_URI=bolt+s://<your-instance-id>.databases.cognodb.cloud
COGNODB_USERNAME=cognodb
COGNODB_PASSWORD=<the password shown once when you created the instance>
